---
name: build

# Actions that take place on the main branch,
# pull-requests to the main branch.

# -----------------
# Control variables (GitHub Secrets)
# -----------------
#
# (n/a)
#
# -----------
# Environment (GitHub Environments)
# -----------
#
# Environment         (n/a)

on:
  push:
    branches:
    - 'master'
  pull_request:
    branches:
    - 'master'

env:
  GITLAB_API: https://gitlab.com/api/v4/projects
  GITLAB_API_TOKEN: ${{ secrets.DATA_MANAGER_TOKEN }}
  GITLAB_PROJECT: 20449765
  OPENAPI_FILE: openapi-0.1.yaml
  OPENAPI_PATH: app/openapi

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Setup node
      uses: actions/setup-node@v2
      with:
        node-version: 16.4.0
    - name: Build and test
      run: |
        curl -H "Private-Token: ${GITLAB_API_TOKEN}" ${GITLAB_API}/${GITLAB_PROJECT}/repository/files/${OPENAPI_PATH}/${OPENAPI_FILE} --output ${OPENAPI_FILE}
        tail ${OPENAPI_FILE}
        npm ci

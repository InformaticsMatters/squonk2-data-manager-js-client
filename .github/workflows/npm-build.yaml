---
name: build

# Actions that take place on the main branch,
# pull-requests to the main branch and external dm-api events.

# -----------------
# Control variables (GitHub Secrets)
# -----------------
#
# (n/a)
#
# -----------
# Environment (GitHub Environments)
# -----------
#
# Environment         (n/a)

on:
  repository_dispatch:
    types:
      dm-api
  push:
    branches:
    - 'master'
  pull_request:
    branches:
    - 'master'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Setup node
      uses: actions/setup-node@v2
      with:
        node-version: 16.4.0
    - name: Build and test
      run: |
        echo ci_pipeline_id=${{ github.event.client_payload.ci_pipeline_id }}
        echo ci_commit_tag=${{ github.event.client_payload.ci_commit_tag }}
        npm ci
